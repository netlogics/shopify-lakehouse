select
    event_id,
    order_id,
    id as line_item_id,
    variant_id,
    product_id,
    customer_id,
    title,
    variant_title,
    name,
    sku,
    vendor,
    quantity,
    fulfillable_quantity,
    current_quantity,
    price,
    total_discount,
    fulfillment_service,
    fulfillment_status,
    requires_shipping,
    taxable,
    gift_card,
    product_exists,
    variant_inventory_management,
    created_at,
    updated_at,
    is_synthetic_fraud,
    fraud_pattern
from {{ source('lakehouse', 'order_details') }}
