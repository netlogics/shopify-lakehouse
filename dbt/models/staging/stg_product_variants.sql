select
    event_id,
    product_id,
    variant_id,
    title,
    price,
    sku,
    "position",
    inventory_policy,
    compare_at_price,
    fulfillment_service,
    inventory_management,
    option1,
    option2,
    option3,
    taxable,
    barcode,
    weight,
    weight_unit,
    inventory_item_id,
    inventory_quantity,
    requires_shipping,
    created_at,
    updated_at
from {{ source('lakehouse', 'product_variants') }}
